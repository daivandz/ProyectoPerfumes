@model ProyectoP.Web.Models.Marca

@{
    ViewBag.Title = "Details";
}
    <div class="container espacio">
        <h2>Detalles</h2>

        <div>
            <h4>Marca</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>

            </dl>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.id }, new { @class = "btn btn-default btn-success" }) |
            @Html.ActionLink("Regresar", "Index",null, new { @class = "btn btn-default btn-info" })
        </p>
</div>